# Newsicera - محاكاة التجارب العلمية 🔬

منصة تفاعلية لمحاكاة التجارب العلمية باستخدام Django، تتيح للمستخدمين استكشاف التجارب العلمية بطريقة تفاعلية وممتعة.

## 🌟 المميزات

- ✅ عرض قائمة التجارب العلمية النشطة
- ✅ صفحات محاكاة تفاعلية لكل تجربة
- ✅ لوحة تحكم إدارية كاملة
- ✅ دعم الصور والوسائط المتعددة
- ✅ تصميم responsive يدعم جميع الشاشات
- ✅ دعم كامل للغة العربية (RTL)
- ✅ إنشاء تلقائي لقوالب المحاكاة عند إضافة تجربة جديدة

## 🛠️ التقنيات المستخدمة

- **Backend**: Python 3.x
- **Framework**: Django 4.1
- **Database**: SQLite
- **ORM**: Django ORM
- **Media Handling**: Pillow

## 📋 المتطلبات

- Python 3.8 أو أحدث
- pip (مدير الحزم)

## 🚀 التثبيت والإعداد

### 1. استنساخ المشروع
```bash
git clone <repository-url>
cd newsicera
```

### 2. إنشاء بيئة افتراضية (اختياري لكن موصى به)
```bash
python -m venv venv

# على Windows
venv\Scripts\activate

# على Linux/Mac
source venv/bin/activate
```

### 3. تثبيت المتطلبات
```bash
pip install -r requirements.txt
```

### 4. تشغيل Migrations
```bash
python manage.py migrate
```

### 5. إنشاء حساب مدير (للوصول للوحة التحكم)
```bash
python manage.py createsuperuser
```

### 6. تشغيل السيرفر
```bash
python manage.py runserver
```

الآن يمكنك فتح المتصفح والذهاب إلى: `http://localhost:8000`

## 📁 هيكل المشروع

```
newsicera/
├── experiments/              # تطبيق التجارب
│   ├── models.py            # موديل Experiment
│   ├── views.py             # Views للصفحات
│   ├── urls.py              # URL routing
│   ├── admin.py             # تسجيل لوحة التحكم
│   ├── signals.py           # إنشاء تلقائي للقوالب
│   ├── templates/           # القوالب
│   │   └── experiments/
│   │       ├── base.html
│   │       ├── index.html
│   │       ├── simulation_base.html
│   │       └── simulations/  # قوالب المحاكاة
│   └── static/              # الملفات الساكنة
│       └── experiments/
│           ├── style.css
│           └── script.js
├── newsicera_project/       # إعدادات المشروع
│   ├── settings.py
│   ├── urls.py
│   └── wsgi.py
├── media/                   # الملفات المرفوعة
├── db.sqlite3              # قاعدة البيانات
├── manage.py               # أداة إدارة Django
└── requirements.txt        # المتطلبات

```

## 🔌 API Endpoints

| Method | Path | Description | Auth Required |
|--------|------|-------------|---------------|
| GET | `/` | الصفحة الرئيسية - قائمة التجارب النشطة | ❌ |
| GET | `/experiment/<slug>/play/` | صفحة محاكاة التجربة | ❌ |
| GET | `/admin/` | لوحة التحكم الإدارية | ✅ |

## 💡 الاستخدام

### إضافة تجربة جديدة

1. افتح لوحة التحكم: `http://localhost:8000/admin/`
2. سجل دخول باستخدام حساب المدير
3. اذهب إلى "التجارب" → "إضافة تجربة"
4. املأ البيانات المطلوبة:
   - عنوان التجربة
   - وصف التجربة
   - رابط التجربة (slug) - سيتم إنشاؤه تلقائياً
   - صورة التجربة (اختياري)
   - تفعيل/إلغاء تفعيل
5. اضغط "حفظ"

سيتم إنشاء ملف HTML تلقائياً للمحاكاة في:
`experiments/templates/experiments/simulations/<slug>.html`

### تخصيص صفحة المحاكاة

بعد إنشاء التجربة، يمكنك تعديل ملف القالب الخاص بها وإضافة:
- كود HTML للواجهة
- JavaScript للمحاكاة التفاعلية
- CSS للتنسيق

## 🎨 التخصيص

### تعديل الألوان والتصميم
عدل ملف: `experiments/static/experiments/style.css`

### إضافة تفاعلات جديدة
عدل ملف: `experiments/static/experiments/script.js`

## 🐛 معالجة الأخطاء

المشروع يتضمن معالجة محسنة للأخطاء:
- صفحة 404 مخصصة للصفحات غير الموجودة
- قالب احتياطي للتجارب التي لا تحتوي على قالب محاكاة مخصص
- رسائل خطأ واضحة وصديقة للمستخدم

## 📝 ملاحظات مهمة

- تأكد من تشغيل `collectstatic` قبل النشر على الإنتاج
- غير `DEBUG = False` في settings.py عند النشر
- استخدم قاعدة بيانات أقوى من SQLite في الإنتاج (PostgreSQL موصى بها)
- احمِ `SECRET_KEY` ولا تشاركها علناً

## 📄 الترخيص

جميع الحقوق محفوظة © 2024

## 👨‍💻 المساهمة

للمساهمة في المشروع:
1. Fork المشروع
2. أنشئ فرع جديد للميزة
3. قم بعمل Commit للتغييرات
4. ادفع إلى الفرع
5. افتح Pull Request

## 📞 الدعم

للأسئلة والدعم، يرجى فتح Issue في المستودع.
